---
source: crates/templates/tests/run_test.rs
expression: output
---
run() {
  declare -A args=()
  declare -a input=()
  normalize_input "$@"
  parse_arguments "${input[@]}"
  # Call the root command
  root_command
  # Call the right command action
  case "$action" in
    "bar")
      parse_bar_arguments "${input[@]}"
      shift $#
      bar
      ;;
  
    "baz")
      parse_baz_arguments "${input[@]}"
      shift $#
      baz
      ;;
  
    "foo")
      parse_foo_arguments "${input[@]}"
      shift $#
      foo
      ;;
  esac
}

