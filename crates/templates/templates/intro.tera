{%- if rules is not defined or rules is not containing("no-disclaimer") -%}# This script was generated by rargs {{ rargs_version | default(value="0.0.0") }} (https://rargs.cloudbridge.uy)
# Modifying it manually is not recommended{%- endif %}

{% if rules is not defined or rules is not containing("no-check-bash-v4") -%}if [[ "${BASH_VERSINFO:-0}" -lt 4 ]]; then
  printf "bash version 4 or higher is required\n" >&2
  exit 1
fi{%- endif %}
