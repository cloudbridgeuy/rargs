while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      {% if not name -%}
      -v | --version)
        version
        exit
        ;;
      {%- endif %}
      -h | --help)
        usage
        exit
        ;;

      *)
        break
        ;;
    esac
  done
